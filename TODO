 * Investigate why crashes when using `join_all()` for all async functions:
   thread 'main' panicked at 'range end index 44 out of range for slice of
   length 8', `netlink-proto-0.6.0/src/codecs.rs:176:64`
